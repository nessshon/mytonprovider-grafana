apiVersion: 1

providers:
  - name: 'Go-backend Dashboards — mytonprovider.org'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /etc/grafana/dashboards/go-backend/mtpo.json
    updateIntervalSeconds: 30
    allowUiUpdates: true
    editable: true

  - name: 'Go-backend Dashboards — mytonstorage.org'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /etc/grafana/dashboards/go-backend/mtso.json
    updateIntervalSeconds: 30
    allowUiUpdates: true
    editable: true

  - name: 'Go-backend Dashboards — gateway'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /etc/grafana/dashboards/go-backend/gateway.json
    updateIntervalSeconds: 30
    allowUiUpdates: true
    editable: true

  - name: 'Providers'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /etc/grafana/dashboards/providers.json
    updateIntervalSeconds: 30
    allowUiUpdates: true
    editable: true

  - name: 'Providers - Details'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /etc/grafana/dashboards/providers-details.json
    updateIntervalSeconds: 30
    allowUiUpdates: true
    editable: true

  - name: 'Providers - Versions'
    orgId: 1
    folder: ''
    type: file
    options:
      path: /etc/grafana/dashboards/providers-versions.json
    updateIntervalSeconds: 30
    allowUiUpdates: true
    editable: true

  - name: 'Per-Provider Dashboards'
    orgId: 1
    folder: 'Per Provider Dashboards'
    type: file
    options:
      path: /etc/grafana/dashboards/per-provider
    updateIntervalSeconds: 30
    allowUiUpdates: true
    editable: true
